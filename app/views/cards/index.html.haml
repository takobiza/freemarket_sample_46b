= render 'shared/main-header'

%main.l-container
  .l-container__menu
    = render 'shared/mypage-sidemenu'

    - if current_user.pay_id.present?
      = render 'mypage-card'
    - else
      = render 'mypage-add-card'

= render 'shared/main-footer'
