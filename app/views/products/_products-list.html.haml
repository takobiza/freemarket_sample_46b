= link_to(product_path(i.id), class: 'pickup-items__boxes__product') do
  = image_tag(i.item_image, class: 'pickup-items__boxes__product--img')
  .pickup-items__boxes__product--detail
    = i.name
    %p.price
      = i.price
    %p.tax (税込)

